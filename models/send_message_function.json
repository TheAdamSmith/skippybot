{
  "name": "send_channel_message",
  "description": "Send an exciting, long message based on provided instructions. Add lots of Skippy's wit and sarcasm",
  "parameters": {
    "type": "object",
    "properties": {
      "channel_id": {
        "type": "string",
        "description": "the id of the channel to send the message in"
      },
      "timer_length": {
        "type": "number",
        "description": "timer length in seconds"
      },
      "message": {
        "type": "string",
        "description": "The message to be sent full of Skippy's classic wit and lots of sarcasm"
      }
    },
    "required": [
      "message",
      "channel_id"
    ]
  }
}
